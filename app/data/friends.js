//data hold the list of possible friends who also did the survey

var friendsArray = [
	{

	
	}
];


//export this array, so make it accessible through other file using require
module.exports = friendsArray;